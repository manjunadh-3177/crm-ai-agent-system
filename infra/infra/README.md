# Infrastructure

This directory is intentionally minimal in Stage 1.

Planned contents for later stages:

- `docker-compose.yml` - full development stack
- `docker-compose.prod.yml` - Lightsail pilot deployment
- `lightsail/` - bootstrap scripts for AWS Lightsail deployment
- `helm/` - Kubernetes Helm chart work
